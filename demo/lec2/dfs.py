"""Depth-first search agent.

Demo of lecture 2. The search itself is in `search.py`.
"""

from search import DepthFirstAgent


class PacmanAgent(DepthFirstAgent):
    """A Pacman agent based on depth-first search."""
